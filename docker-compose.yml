services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ruang
      POSTGRES_PASSWORD: ruang_dev
      POSTGRES_DB: ruang
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
